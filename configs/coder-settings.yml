# CODER-specific runtime settings.

workspace-agents:
  # When enabled, CODER sessions read the workspace rules file from
  # <workspaceConfig.root>/<file> and inject it into the system prompt.
  enabled: true
  file: AGENTS.md
